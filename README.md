# Toy 2D game editor written in Scala and OpenGL

To build, in the terminal:

    $ ./sbt

In the SBT shell:

    > deploy
    > exit

In the terminal again:

    $ cd deploy
    $ chmod a+x editor
    $ ./editor -w test --Srender.shadows true

Enjoy.